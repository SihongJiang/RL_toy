env:
  name: CartPole-v1

seed: 0

model:
  hidden_dim: 128

optimizer:
  actor_lr: 0.0003
  critic_lr: 0.0005

train:
  num_updates: 100
  rollout_steps: 1024
  minibatch_size: 64
  ppo_epochs: 10
  gamma: 0.99
  gae_lambda: 0.95
  clip_eps: 0.2
  vf_coef: 0.5
  ent_coef: 0.01
  max_grad_norm: 0.5